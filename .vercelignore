# Only paths that are neither a workspace package nor part of the build.
# Do not exclude anything under artifacts/ or lib/: pnpm install --frozen-lockfile
# needs every workspace package the lockfile knows about.
.conversation
.replit
.replitignore
replit.md
**/node_modules
**/dist
