{
  "name": "@void/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Native Tauri desktop shell for the shared VOID workbench.",
  "scripts": {
    "prepare:runtime": "node scripts/prepare.mjs",
    "dev": "pnpm prepare:runtime && tauri dev",
    "build": "pnpm prepare:runtime && node scripts/build.mjs",
    "test": "node --test runtime.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "esbuild": "0.28.2"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.33.0",
  "keywords": [
    "void",
    "desktop",
    "tauri",
    "agents"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Dawe583/void.git",
    "directory": "apps/desktop"
  },
  "homepage": "https://github.com/Dawe583/void/tree/main/apps/desktop"
}
