# Verification: message-inventory.md

Every bullet names what was read or run, where, and what it showed. All paths are under `packages/proxy/node_modules/@modelcontextprotocol/sdk/dist/esm/` (the `sdk/` prefix in the inventory). Run from the repository root.

1. Read `sdk/types.js` in full, lines 1-2065, with line numbers. Confirmed: protocol version constants at lines 2-4 (`LATEST_PROTOCOL_VERSION = '2025-11-25'`, `DEFAULT_NEGOTIATED_PROTOCOL_VERSION = '2025-03-26'`, the 5-entry `SUPPORTED_PROTOCOL_VERSIONS`), `JSONRPC_VERSION = '2.0'` at line 7, `ErrorCode` enum at lines 158-171, every request/notification/result schema cited in inventory sections 1-4, and the direction unions `ClientRequestSchema` (1956-1974), `ClientNotificationSchema` (1975-1981), `ClientResultSchema` (1982-1991), `ServerRequestSchema` (1993-2002), `ServerNotificationSchema` (2003-2013), `ServerResultSchema` (2014-2028).

2. Read `sdk/shared/protocol.js` in full, lines 1-1107, with line numbers. Confirmed: `DEFAULT_REQUEST_TIMEOUT_MSEC = 60000` at line 8, sequential integer request ids (`_requestMessageId` at line 16, `id: messageId` at 637-641), progress token injection `_meta.progressToken = messageId` at 643-652, cancel sends `notifications/cancelled` with reason at 670-687, `_onclose` aborts controllers and rejects pending with ConnectionClosed at 248-269, MethodNotFound reply for unknown methods at 284-313, error code passthrough with InternalError fallback at 389-402, strict-envelope discrimination in `connect` at 215-247.

3. Read `sdk/server/index.js` in full (lines 1-429) and `sdk/client/index.js` in full (lines 1-613). Confirmed: initialize handling and version echo-or-latest on the server (259-263, plus 52-53 for installed handlers), client initialize flow with `notifications/initialized` and close-on-failure (274-317), capability assertion switch tables on both sides (server 140-247, client 336-443), the `tools/call` outputSchema validation on the client (479-510), elicitation mode normalization (client 60-69, 180-232), and the `listChanged` auto-refresh defaults (client 106-139, 560-608).

4. Read `sdk/shared/stdio.js` (lines 1-40), `sdk/server/stdio.js` (lines 1-81), `sdk/client/stdio.js` (lines 1-194) in full. Confirmed: newline framing with trailing `\r` strip and 10 MiB default max buffer in `ReadBuffer` (2-36), drain-event backpressure in both `send()` implementations (server 69-79, client 179-192), the client close ladder stdin.end -> 2000 ms -> SIGTERM -> 2000 ms -> SIGKILL (143-178), default inherited env allowlist HOME/LOGNAME/PATH/SHELL/TERM/USER with `()`-prefixed value filtering (8-42), and PassThrough stderr available before spawn (48-56, 103-120).

5. Read `sdk/shared/transport.js` (lines 1-39), `sdk/spec.types.js` (lines 1-24) in full, plus `sdk/shared/transport.d.ts` (lines 1-89) and `sdk/shared/protocol.d.ts` (lines 1-443) for the `Transport` and `Protocol` interface contracts (TransportSendOptions, onclose/onerror semantics, RequestOptions timeout docs at protocol.d.ts:61-98). Confirmed the internal `"DRAFT-2026-v1"` constant at spec.types.js:12 is separate from the negotiated version list and never used for negotiation.

6. Ran grep for request-id generation to disprove "UUID ids": `grep -rn "randomUUID" dist/esm/` returns 29 hits, all HTTP session/stream ids or auth handlers (e.g. `server/sse.js:21`, `server/streamableHttp.js` docs, `server/auth/handlers/register.js:52`), none in `shared/protocol.js`. Confirmed request ids are `_requestMessageId++` integers only.

7. Ran grep for signal handling in stdio transports: `grep -n "SIGTERM\|SIGINT\|process.on" dist/esm/server/stdio.js dist/esm/client/stdio.js` returns exactly one hit, SIGTERM inside `StdioClientTransport.close()` (client/stdio.js:161); the SIGKILL step at line 170 is covered by the full read in bullet 4. Confirmed `StdioServerTransport` installs no process signal handlers; host process owns SIGTERM/SIGINT.

8. Ran grep for the complete method inventory: `grep -n "z.literal('notifications/" dist/esm/types.js` returns exactly 11 notification methods (cancelled, progress, tasks/status, resources/list_changed, resources/updated, prompts/list_changed, tools/list_changed, message, roots/list_changed, elicitation/complete, initialized), matching inventory section 3; `grep -n "method: z.literal(" dist/esm/types.js` returns every request method and matches the union schemas in bullet 1. Cross-checked counts: 17 client-to-server request methods, 8 server-to-client (ping shared, 20 unique total), 11 notifications (3 both-direction, 2 client-to-server, 6 server-to-client).

9. Ran grep for capability assertion paths to map direction gating: `grep -n "assertCapabilityForMethod\|assertNotificationCapability\|assertRequestHandlerCapability" dist/esm/client/index.js dist/esm/server/index.js` confirms the switch tables cited in sections 2-3, including `notifications/cancelled` and `notifications/progress` being always allowed on both sides and `notifications/elicitation/complete` requiring `elicitation.url`.

10. Checked the package version: `grep -m1 version packages/proxy/node_modules/@modelcontextprotocol/sdk/package.json` returns `1.30.0`, and `grep -n "zod/v4" dist/esm/types.js` at line 1 confirms zod v4 is the schema runtime. Both match the pinned dependency in `packages/proxy/package.json`.

No facts in `message-inventory.md` come from the MCP prose specification, memory, or the `.d.ts` type-only surface alone; each is tied to implementation lines read above.
